import { productsFeature } from './products.reducer';

export const {
  selectProducts,
  selectSelectedProduct,
  selectVariations,
  selectCategories,
  selectLoading,
  selectSaving,
  selectError,
  selectFilters,
  selectPagination,
} = productsFeature;
